# OpenAI API Key for GPT-4
OPENAI_API_KEY=your_openai_api_key_here

# Set to 'true' to disable PDF uploads (for demo mode)
DISABLE_PDF_UPLOADS=false
NEXT_PUBLIC_DISABLE_PDF_UPLOADS=false

# Optional: Set custom server host (defaults to 'localhost' if not set)
NEXT_PUBLIC_SERVER_HOST=localhost 